{
    "configurations": [
        {
            "type": "debugpy",
            "request": "launch",
            "name": "Launch App",
            "program": "${workspaceFolder}/app.py"
        }
    ],
}